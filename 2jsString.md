```javascript
'I\'m \"OK\"!';
'\x41'; // 完全等同于 'A'
'\u4e2d\u6587'; // 完全等同于 '中文'
'\n'
`这是一个
多行
字符串`;
// 如果浏览器不支持ES6，将报SyntaxError错误

var s = 'Test';
s[0] = 'X';
alert(s); // s仍然为'Test'
//字符串是不可变的,JavaScript为字符串提供了一些常用方法，注意，调用这些方法本身不会改变原有字符串的内容，而是返回一个新字符串

```
